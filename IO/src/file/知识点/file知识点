File类
    1介绍
        文件和目录路径名的抽象表示
        对于File来讲，其封装的并不是一个真正的文件，仅仅是一个路径名。可以是存在的，也可以是不存在的。将来是要将具体的操作把这个路径的内容转化为具体存在
    2构造方法  常见3种
    3创建功能  常见3种
            File f1 = new File("E:\\JavaZJ\\IO\\src\\file\\创建功能\\file.txt");
            boolean mkdirFile = f1.mkdir();
            注意 本意是想创建文件，通过mkdir创建的是目录名为file.txt的文件目录
    4判断和获取功能
        4.1判断功能 常见3种
        4.2获取功能 常见5种